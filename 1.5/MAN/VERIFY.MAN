}
  VERIFY ON|OFF

Low Memory

This external command switches the
write with verify option ON or OFF.
The default setting is OFF. When the
option is ON, every written sector is
read back internally by the drive and
the data is compared. This makes
writing significantly slower, but it
can be useful when working with disks
of poor quality.
