}
  SAVE file start end [/A]

Low Memory

This command saves the memory area
defined by the start and end addresses
as a DOS loadable file. With the /A
option it appends the data to an
existing file as an additional
segment.

Both start and end address must be
specified as hexadecimal numbers.
