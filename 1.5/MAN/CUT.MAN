}
  CUT file1 file2 position [length]

This external command saves a chosen
section of file1 into file2. When no
length is provided, the command saves
the maximum range, starting from the
given position up to the file's end.

Both position and length must be
specified as hexadecimal numbers.

