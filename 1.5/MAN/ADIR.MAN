}
  ADIR [Dn:[sub_1:..:sub_n:]pattern]

This program lists directories of
Atari DOS, MyDOS, and compatible
disks. Paths must be specifed in MyDOS
notation, meaning : is used as the
directory separator instead of >.

The following mark characters are
used:
:  marks directories
*  marks protected files or dirs
