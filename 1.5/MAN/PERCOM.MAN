}
  PERCOM [D:x] ..

Reads the Percom block from drives
that support it. If no drive number is
given, the current drive is used. If
the drive does not support Percom
blocks, error 139 (Device NAK) is
returned.
