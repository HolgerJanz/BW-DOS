}
  TYPE file ..

Low Memory, Internal

This command shows the contents of a
file, for example text or batch files.

The internal version does not support
a list of parameters only the first
parameter is evaluated.
