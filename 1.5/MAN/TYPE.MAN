}
  TYPE file ..

Low Memory, Internal

This command displays the contents of a
file, such as text or batch files.

The internal version does not support
multiple parameters, only the first
parameter is processed.
