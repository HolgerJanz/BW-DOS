}
  PWD [Dn:] ..

Lists the current directory of a drive
(prints the working directory). If no
drive is specified, the current drive
is used. The current directory can be
changed with the CD command.
