}
  RD [Dn:][path>]name ..

Low Memory, Internal

This command deletes directories. Only
empty directories can be removed; if a
directory contains files or other
subdirectories, they must be deleted
first.

See the DEL command for recursive
deletion of directories and files.

The internal version does not support
multiple parameters, only the first
one is processed.
