}
  BASIC ON|OFF

Low Memory, Internal

Switches the internal BASIC (XL/XE
only) on or off. It works only when
no external cartridge is installed.
Memory, including BASIC programs,
is erased. See the CAR command to
start BASIC.

The external version preserves the
color register.
