}
  APETIME

Low Memory

This command reads time and date
via SIO from supported external
devices and sets the BW-DOS clock.
Known devices supporting the
APETIME SIO command include APE,
SIO2PC, and FujiNet. Together with
CLOCK.SYS it can provide current
file time and date stamps.
