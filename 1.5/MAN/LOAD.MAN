}
  LOAD file ..

Low Memory, Internal

This command simply loads a DOS
loadable file (program). Any init or
run address contained in the file is
ignored.

The external version occupies the
memory area from $580 to $6FF, so
programs cannot be loaded into this
region when using that version.

The internal version does not support
a list of parameters.
