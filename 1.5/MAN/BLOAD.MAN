}
  BLOAD file address [length]

Low Memory

This is the external command for
loading data files (non-DOS
loadable) into memory. If no
length is specified, the entire
file is loaded. Both address and
length must be specified as
hexadecimal numbers.
