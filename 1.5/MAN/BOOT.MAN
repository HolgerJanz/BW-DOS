}
  BOOT file

With this command you can specify a
file to be loaded when the system is
powered on (booting). Commonly it is a
DOS file, but other files can also be
booted. The file must not be loaded to
memory area $2E00-$317F, where the
loader itself is located.
