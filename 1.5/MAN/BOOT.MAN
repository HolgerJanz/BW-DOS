}
  BOOT file

With this command you can specify a
file to be loaded when the system is
powered on. It is usually a DOS file,
but other files can also be booted.
The file must not load into memory
area $2E00-$317F, where the loader
itself resides.
