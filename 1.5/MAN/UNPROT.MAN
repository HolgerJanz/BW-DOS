}
  UNPROT file

Low Memory, Internal

This command removes protection from
files or directories. Using wildcards,
you can unprotect multiple files and
directories at once. To protect files
or directories, use the PROT command.
